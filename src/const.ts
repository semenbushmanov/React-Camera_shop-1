export enum AppRoute {
  Root = '/',
  Item = '/item',
  Basket = '/basket',
}

export enum APIRoute {
  Cameras = '/cameras',
  Promo = '/promo',
  Reviews = '/reviews',
  Coupons = '/coupons',
  Orders = '/orders',
}

export enum NameSpace {
  Data = 'DATA',
  User = 'USER',
}
